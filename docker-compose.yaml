services:
  mongo:
    image: mongo:latest
    container_name: mongo_container
    environment:
      MONGO_INITDB_ROOT_USERNAME: jon
      MONGO_INITDB_ROOT_PASSWORD: jon
    ports:
      - "27017:27017"
    volumes:
      - ./mongo_data:/data/db